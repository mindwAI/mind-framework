---
status: done
feature_id: feat-001-todo-app
type: qa
created: 2026-01-21
---

# QA Report — Todo App API

## Verdict: PASSED

## Review Bug Fixes Verified

Issues #1 and #2 from review-001 were fixed before this QA run:

| Review Issue | Fix Verified | Evidence |
|-------------|-------------|----------|
| #1 — Non-numeric :id not validated | Fixed | `PATCH /todos/abc/complete` now returns 404 without hitting DB. Test added. |
| #2 — Whitespace-only text accepted | Fixed | `POST /todos` with `{ "text": "   " }` now returns 400. Trim applied before validation. Test added. |

## Functional Requirements Checklist

| FR | Description | Status | Evidence |
|----|-------------|--------|----------|
| FR-1.1 | POST creates todo, returns full object | PASS | curl POST returns 201 with id, text, completed_at, created_at |
| FR-1.2 | POST rejects missing/empty/long text | PASS | 400 for missing, empty, whitespace-only, and 501-char text |
| FR-1.3 | POST returns 201 on success | PASS | Status code verified in integration test |
| FR-2.1 | GET returns all todos, ordered desc | PASS | 3 todos returned in reverse creation order |
| FR-2.2 | GET ?completed=true filters | PASS | Only completed items returned |
| FR-2.3 | GET ?completed=false filters | PASS | Only pending items returned |
| FR-2.4 | GET returns empty array if none | PASS | Fresh DB returns 200 [] |
| FR-3.1 | PATCH sets completed_at timestamp | PASS | ISO timestamp set, verified non-null |
| FR-3.2 | PATCH returns 404 for missing id | PASS | id=99999 returns 404 |
| FR-3.3 | PATCH returns 200 with updated object | PASS | Response includes completed_at |
| FR-3.4 | PATCH is idempotent | PASS | Second complete returns 200, same timestamp |

**11/11 FRs PASS.**

## Automated Tests

```
Test Suites: 2 passed, 2 total
Tests:       18 passed, 18 total
Coverage:    87% lines (src/)
```

All tests pass. No flaky tests detected across 3 runs.

## Additional Checks

| Check | Status |
|-------|--------|
| Error responses use `{ "error": "..." }` format | PASS |
| No stack traces leaked in error responses | PASS |
| Database persists across server restart | PASS |
| No unhandled promise rejections | PASS |

## Conclusion

All 11 functional requirements verified. Both review bugs confirmed fixed
with regression tests added. Test coverage at 87% exceeds the 80% target.
The todo app API is complete and meets all acceptance criteria.
